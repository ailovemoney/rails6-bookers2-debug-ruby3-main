<!--id="comments_areaにをrenderで指定した部分テンプレートに更新-->
$('.comments_area').html("<%= j(render 'book_comments/comment-list', books: @book ) %>");
$('.new-comment').html("<%= j(render 'book_comments/comment-form', books: @book, book_comment: @book_comment ) %>");

<!--フォームを送信したあとにフォームの値を空にします-->
$("textarea").val('');